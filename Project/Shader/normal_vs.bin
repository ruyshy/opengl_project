#version 460 core
layout (location = 0) in vec2 aPos;

uniform mat4 projection;
uniform mat4 model_matrx;
uniform mat4 view_matrx;

uniform float z_buf;

void main()
{
	gl_Position = projection * view_matrx * model_matrx * vec4(aPos.x, aPos.y, z_buf + 0.00001f, 1.0);
};